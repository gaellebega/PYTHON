n=input()
x=float(input())
y=int(input())

# to get the multiple numbers in one line

# when we want  to get the input from the user 
# when we want the list to become as a string we just do like this
numbers=list(map(int,input().split()))
# we use only map when we only know the number of the elements
m,p=map(int,input().split())
# so when you want it to be like at how many times you have to use the for loop
print(n) 
print(x)
print(y)
print(numbers)
print(m,p)

# print with the end is used to make the variable not go to the next line but the variable be to one another

